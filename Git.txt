git config --global user.name
git config --global user.email

git init (intialize)
git add . (all)
git add .\FilePath\

git diff // Gives All Diff From Previous Stage to Current Stage
git rm --cached FileName // remove from tracking but file remain on your system
git rm -f FileName // remove from tracking + remove from your system

git log // Provided details about commit id , author , date , commit message , email
git show hashCode // Give Detail about that commit

git blame FilePath  // You can blame that whose has written that code

git status // It tells which file is modified in your local in and which files in staging area
// staging area means that where git has info about the added code or code which is ready to commit 

Head always points to latest commit 
If commit is done in local but not push in github

if we need to remove the commited code also use
    git reset --hard HEAD~1   ye code plus commit history top ki remove kr dega only in local

if we need to remove only commit history but the code should remain unchnaged
    git reset --soft HEAD~1

If commit is done in local and also pushed to github then use
git revert hashCode

ye sare chnages uske undo krdega for exmaple + let count = 5 toh revert usko - let count = 5 krdega or ek new history bngya

Agr github se bhi wo commit history htani h toh 
Phele apne local system me reset krke usme revet back kro using git reset --hard / --soft HEAD~1
HEAD~1 means ek phele wale repo me jao

ab iske bad isko forcefully push kro and that's all
git push -f origin main

Branching is basically help us to make the commits very clear and concise but why ? let consider everyone is psuhing on the same branch
this will make the commits very messy and buggy which further leads to 2 major problems or merging and conficts as result there will the delay in the development
of the software beacuse developers will spend more time in merging instead of business logic


# Create the branch
git branch <branch Name>

# switch branch
git checkout branch Name

# git merge switch back to main first important
git merge origin/branchName

# Mergeing leads to an issue when one branch is merged with the main one then the all the commits of that branch are combine over one commit and this commit is attach
on the head so if we need to revert back on the created branch commit we cannot do this because we have lost out the history

# Rebase means that we are connecting the whole branch at the head and head will point to the new brach and head points to the last node of the merged branch
issue isme ye h bas ki commit history bhot lambi ho jygi or kuch nhi